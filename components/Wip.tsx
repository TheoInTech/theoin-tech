import React from "react";

const Wip = () => {
  return (
    <section className="font-medium text-lg text-center h-full w-full flex justify-center items-center">
      wip.
    </section>
  );
};

export default Wip;
